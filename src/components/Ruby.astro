---
type Props = {
    text: string[],
    ruby: string[],
}

const { text: textSets, ruby: rubySets } = Astro.props;
---
<ruby translate="no">
    { textSets.map((textSet, i) => <>{textSet} <rp>(</rp><rt>{rubySets[i]}</rt><rp>)</rp></>) }
</ruby>